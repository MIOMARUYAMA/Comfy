<main>
  <div class="row justify-content-center my-5">
    <h3>本当に退会しますか？</h3>
  </div>
  <div class="row justify-content-center mt-3">
    <div class="col-sm-2">
      <%= link_to '退会しない', end_user_path(current_end_user.id), {class: "btn btn-primary"} %>
    </div>
    <div class="col-sm-2">
      <%= link_to  end_users_withdrawal_path, method: :patch do %>
        <button class="btn btn-danger">退会する</button>
      <% end %>
    </div>
  </div>
</main>
